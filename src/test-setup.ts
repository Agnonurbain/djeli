// src/test-setup.ts

/**
 * Configuration Vitest — chargé avant chaque suite de tests.
 * Ajoute les matchers jest-dom (toBeInTheDocument, toHaveTextContent, etc.)
 */

import '@testing-library/jest-dom';
